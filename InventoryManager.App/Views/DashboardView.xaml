<UserControl x:Class="InventoryManager.App.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="16">
            <TextBlock Text="Dashboard" Style="{StaticResource PageTitle}"/>

            <UniformGrid Columns="4" Rows="2">
                <Border Background="#F3F4F6" Margin="4" Padding="16" CornerRadius="6">
                    <StackPanel>
                        <TextBlock Text="Total Products" FontSize="12" Foreground="Gray"/>
                        <TextBlock Text="{Binding TotalProducts}" FontSize="28" FontWeight="Bold"/>
                    </StackPanel>
                </Border>

                <Border Background="#FEF3C7" Margin="4" Padding="16" CornerRadius="6">
                    <StackPanel>
                        <TextBlock Text="Low Stock" FontSize="12" Foreground="Gray"/>
                        <TextBlock Text="{Binding LowStockCount}" FontSize="28" FontWeight="Bold"/>
                    </StackPanel>
                </Border>

                <Border Background="#FEE2E2" Margin="4" Padding="16" CornerRadius="6">
                    <StackPanel>
                        <TextBlock Text="Out of Stock" FontSize="12" Foreground="Gray"/>
                        <TextBlock Text="{Binding OutOfStockCount}" FontSize="28" FontWeight="Bold"/>
                    </StackPanel>
                </Border>

                <Border Background="#F3F4F6" Margin="4" Padding="16" CornerRadius="6">
                    <StackPanel>
                        <TextBlock Text="Total Suppliers" FontSize="12" Foreground="Gray"/>
                        <TextBlock Text="{Binding TotalSuppliers}" FontSize="28" FontWeight="Bold"/>
                    </StackPanel>
                </Border>

                <Border Background="#DCFCE7" Margin="4" Padding="16" CornerRadius="6">
                    <StackPanel>
                        <TextBlock Text="Inventory Value" FontSize="12" Foreground="Gray"/>
                        <TextBlock Text="{Binding InventoryValue, StringFormat=C}" FontSize="24" FontWeight="Bold"/>
                    </StackPanel>
                </Border>

                <Border Background="#DBEAFE" Margin="4" Padding="16" CornerRadius="6">
                    <StackPanel>
                        <TextBlock Text="Today's Sales" FontSize="12" Foreground="Gray"/>
                        <TextBlock Text="{Binding TodaysSales, StringFormat=C}" FontSize="24" FontWeight="Bold"/>
                    </StackPanel>
                </Border>

                <Border Background="#DBEAFE" Margin="4" Padding="16" CornerRadius="6">
                    <StackPanel>
                        <TextBlock Text="Today's Profit" FontSize="12" Foreground="Gray"/>
                        <TextBlock Text="{Binding TodaysProfit, StringFormat=C}" FontSize="24" FontWeight="Bold"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <TextBlock Text="Sales Trend (Last 7 Days)" FontSize="16" FontWeight="Bold" Margin="0,24,0,8"/>
            <lvc:CartesianChart Series="{Binding SalesTrendSeries}"
                                 XAxes="{Binding SalesTrendXAxes}"
                                 Height="250"/>

            <TextBlock Text="Top Selling Products" FontSize="16" FontWeight="Bold" Margin="0,24,0,8"/>
            <lvc:CartesianChart Series="{Binding TopProductsSeries}"
                                 XAxes="{Binding TopProductsXAxes}"
                                 Height="250"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>