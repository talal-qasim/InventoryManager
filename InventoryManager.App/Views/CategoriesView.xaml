<UserControl x:Class="InventoryManager.App.Views.CategoriesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:helpers="clr-namespace:InventoryManager.App.Helpers">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Categories" FontSize="22" FontWeight="Bold" Margin="0,0,0,16"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBox Width="180" Margin="0,0,8,0" Padding="6" helpers:PlaceholderHelper.Placeholder="Category Name"
                     Text="{Binding NewCategoryName, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBox Width="240" Margin="0,0,8,0" Padding="6" helpers:PlaceholderHelper.Placeholder="Description"
                     Text="{Binding NewCategoryDescription, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Content="+ Add Category" Command="{Binding AddCategoryCommand}" Padding="8,6"/>
        </StackPanel>

        <TextBlock Grid.Row="2" Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,0,0,8"/>

        <DataGrid Grid.Row="3"
                  ItemsSource="{Binding Categories}"
                  SelectedItem="{Binding SelectedCategory}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="200"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="4" Content="Deactivate Selected" Command="{Binding DeactivateCategoryCommand}"
                Padding="8,6" Margin="0,8,0,0" HorizontalAlignment="Left"/>
    </Grid>
</UserControl>